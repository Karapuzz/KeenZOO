{
  "run_type": "nat",
  "local_addr": "::",
  "local_port": {{localporttrojan}},
  "remote_addr": "{{host}}",
  "remote_port": {{port}},
  "password": ["{{pw}}"],
  "ssl": {
    "verify": {{verify}},
    "verify_hostname": true,
    "sni": "{{sni}}",
    "alpn": ["{{alpn}}"],
    "fingerprint": "{{fp}}"
  },
  "websocket": {
    "enabled": {{ws_enabled}},
    "path": "{{path}}",
    "host": "{{host_header}}"
  }
}